# ============================================================
# UEBA ML — Lockfile de PRODUCTION (versions ÉPINGLÉES)
# ============================================================
# ⚠️ IMPORTANT — source de vérité :
#   `pip install -e .` (Colab, make install) lit les bornes LARGES de
#   pyproject.toml, PAS ce fichier. Ces pins servent à REPRODUIRE sur VM1
#   exactement l'environnement d'entraînement de Colab :
#     - sur VM1 (daemon) :  pip install -r requirements.txt
#   afin que le chargement des .pkl (scaler/IF/OCSVM) ne souffre pas d'un
#   écart de version scikit-learn entre entraînement (Colab) et service (VM1).
#   Idéalement : aligner ces versions sur le `pip freeze` de la session Colab
#   qui a produit les modèles (cf. audit RC-3).
# Installer : pip install -r requirements.txt
# ============================================================

# Data
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
pyyaml==6.0.1
python-dateutil==2.9.0

# Machine Learning
scikit-learn==1.5.0
joblib==1.4.2

# Deep Learning
tensorflow==2.16.1
keras==3.3.3

# Visualisation
matplotlib==3.9.0
seaborn==0.13.2
plotly==5.22.0

# Utilitaires
tqdm==4.66.4
colorama==0.4.6
